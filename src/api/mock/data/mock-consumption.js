import _ from 'lodash';
import facility from './mock-facilities';
import devices from './mock-devices';

// how many devices do we want
// how many types do we want (LED, HVAC, BLINDS, SENSORS)
//
function getConsumptionSummary() {
  const summary = {};
  // for facility
  // per floor
  //  summary
  //  per hour data
  return summary;
}

/*

summary = {
  floors: {
    floorNo: [
      {}
    ]
  }
}

date: {
  summary: {
    total: value,
  }
}

*/
